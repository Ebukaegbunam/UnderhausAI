# Session Log — 2026-04-21

## Landing Page → Vercel

- Committed and deployed landing page to Vercel production
- URL: https://landing-delta-six-40.vercel.app
- `landing/vercel.json` updated: `buildCommand: npm run build`, `outputDirectory: dist`, `framework: vite`
- Landing was refactored from single HTML file to full Vite React app with components:
  `Nav`, `Hero`, `AgentCard`, `Contrast`, `HowItWorks`, `Pricing`, `FinalCTA`, `Footer`
  and hooks: `useCountUp`, `useScrollReveal`

## Backend DB Engine — Supabase Fix

- `backend/db/engine.py` updated to handle asyncpg `statement_cache_size: 0` for Supabase transaction pooler (prepared statements not supported on pooler port 6543)

## Frontend Auth Routing

- `App.jsx` replaced with React Router v7 — routes: `/login`, `/signup`, `/auth/callback`, `/auth/error`, `/dashboard`
- `LoginPage.jsx` — Google + GitHub OAuth buttons; logo links to landing at `VITE_LANDING_URL`
- `AuthCallback.jsx` — reads `?token=` from URL, stores via `auth.setToken`, redirects to dashboard
- `Dashboard.jsx` — placeholder replaced with full investor dashboard (see 2026-04-22)
- `api/client.js` — added `auth` helpers: `setToken`, `clearToken`, `getToken`, `isLoggedIn`; all requests attach `Authorization: Bearer` header
- `frontend/.env.local` — `VITE_LANDING_URL=http://localhost:3001` added (landing runs on port 3001)
- `vite.config.js` — port changed to 5175

## Logo Navigation Fix

- Login page logo was hardcoded to `localhost:5174` — fixed to use `VITE_LANDING_URL` env var
- Dashboard logo was `href="#"` — fixed to `<Link to="/dashboard">`

## Dev Login

- `POST /auth/dev-login` added to backend (only active when `DEV_MODE=true` in `.env`)
- Creates a real JWT for `dev@underhaus.local` without OAuth
- Login page shows a dashed "Continue as Dev User" button only when `import.meta.env.DEV` is true (invisible in production builds)
- `DEV_MODE=true` added to `backend/.env`
